const extractNestedObjectClasses = (key, nestedKey, nestedValue) => {
    // THIS FUNCTION MUST CALL extractClasses PASSING THE nestedKey AND THE nestedValue
    // AND RETURN THE RETURN OF extractClasses
    console.log(key, " --> ", nestedKey, nestedValue);
};

module.exports = { extractNestedObjectClasses };
