/**
 * Parses the Galadriel configuration file (galadriel.config.ts or galadriel.config.js).
 *
 * @returns {object} The parsed configuration object.
 */
declare const parseGaladrielConfig: () => any;
export { parseGaladrielConfig };
